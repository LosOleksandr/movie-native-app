import { Text } from 'react-native'

import SafeViewContainer from '@/components/shared/safe-view-container'

const Page = () => {
    return (
        <SafeViewContainer>
            <Text>index</Text>
        </SafeViewContainer>
    )
}

export default Page
